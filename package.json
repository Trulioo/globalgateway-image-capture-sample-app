{
  "name": "globalgateway-capture-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@trulioo/globalgateway-image-capture-sdk": "^1.1.8",
    "bootstrap": "^4.3.1",
    "react": "^16.12.0",
    "react-bootstrap": "^1.0.0-beta.16",
    "react-dom": "^16.12.0",
    "react-scripts": "3.2.0",
    "react-select": "^3.0.8"
  },
  "scripts": {
    "start": "set HTTPS=true&react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "setup": "bash ./setup.sh",
    "update-gg-capture": "npm run update-gg-sdk && npm run setup",
    "update-gg-sdk": "npm install @trulioo/globalgateway-image-capture-sdk@latest"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
